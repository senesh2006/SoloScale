/**
 * Dev 1 — Gemini strategy generation
 * Replace mock-store strategy with real Gemini API calls.
 */
export async function generateCampaignStrategy(_input: {
  goal_prompt: string;
  title: string;
}) {
  throw new Error("Not implemented — connect GEMINI_API_KEY");
}
