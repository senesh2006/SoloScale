/**
 * Gemini Native image model ("Nano Banana").
 *
 * NOT used for flyers — flyers use free Pixazo Flux 1 Schnell instead.
 * Kept for reference in deprecated `gemini/imageService.ts` only.
 *
 * @see https://ai.google.dev/gemini-api/docs/image-generation
 */
export const GEMINI_IMAGE_MODEL = "gemini-2.5-flash-image" as const;
